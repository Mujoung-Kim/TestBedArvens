package com.todaysquare.patterntasknote.di.modules

import org.koin.core.module.Module
import org.koin.dsl.module

val apiModule = module {
    //  There's only work to parse, so I don't want to use it

}